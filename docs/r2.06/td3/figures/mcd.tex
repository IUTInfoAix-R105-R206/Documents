\documentclass[border=10pt]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

\usepackage{tikz-er2}
\usepackage{pgf-umlcd}

\usetikzlibrary{decorations.text}
\usetikzlibrary{decorations.markings}
\usetikzlibrary{positioning}
\usetikzlibrary{shadows}
\usetikzlibrary{backgrounds}

\tikzstyle{every entity} = [top color=white,
                            bottom color=blue!30,
                            draw=blue!50!black!100,
                            drop shadow]

\tikzstyle{every weak entity} = [drop shadow={shadow xshift=.7ex, shadow yshift=-.7ex}]

\tikzstyle{every attribute} = [top color=white,
                               bottom color=yellow!20,
                               draw=yellow,
                               node distance=1cm,
                               drop shadow]

\tikzstyle{every relationship} = [top color=white,
                                  bottom color=yellow!20,
                                  draw=yellow,
                                  drop shadow]

\tikzstyle{every isa} = [draw=blue!50!black!100]

\tikzset{card/.style={font=\scriptsize\sffamily, fill=white, inner sep=1pt}}

\begin{document}
\begin{tikzpicture}[node distance=1.96cm, every edge/.style={link}]

  \node[entity] (mat) {
    \textbf{Module}
    \nodepart{second}
    \key{code}\\
    libelle\\
    heureCMPrev\\
    heureCMReal\\
    heureTDPrev\\
    heureTDReal\\
    discipline\\
    coefCC\\
    coefTest\\
  };

  \node[entity] (etud) [below right =of mat ] {
    \textbf{Etudiant}
    \nodepart{second}
    \key{numEt}\\
    nomEt\\
    prenomEt\\
    cpEt\\
    villeEt\\
    anneeEt\\
    groupeEt\\
  };

  \node[entity] (ens) [above right=of etud ] {
    \textbf{Prof}
    \nodepart{second}
    \key{numProf}\\
    nomProf\\
    prenomProf\\
    adresseProf\\
    cpProf\\
    villeProf\\
  };

  \node[relationship] (notation) [below =of mat] {Notation}
     child {node[attributes] {moyCC\\moyTest}};
  \draw[link] (mat.south) -- node [pos=0.15, auto] {(0,n)} (notation);
  \draw[link] (etud.145) -- node [pos=0.3, auto, swap] {(0,n)} (notation);

  \node[relationship] (enseigne) [above=of etud] {Enseignement};
  \draw[link] (mat.317) -- node [pos=0.4, auto, swap] {(0,n)} (enseigne.west);
  \draw[link] (ens.205) -- node [pos=0.4, auto] {(0,n)} (enseigne.east);
  \draw[link] (etud.north) -- node [pos=0.15, auto, swap] {(0,n)} (enseigne);

  \node[relationship] (mat_spec) [above=1cm of enseigne] {Spécialiste};
  \draw[link] (mat.20) -- node [pos=0.3, auto, swap] {(0,n)} (mat_spec);
  \draw[link] (ens.140) -- node [pos=0.3, auto] {(1,1)} (mat_spec);

  \node[relationship] (resp) [above=1.7cm of mat_spec] {Responsable};
  \draw[link] (mat.north) |- node [pos=0.15, auto, swap] {(1,1)} (resp);
  \draw[link] (ens.north) |- node [pos=0.07, auto] {(0,n)} (resp.east);

  \node[relationship] (mat_pere) [left=1cm of mat] {A pour père};
  \draw[link] (mat.150) -| node [pos=0.1, auto, swap] {(1,1)} (mat_pere.north);
  \draw[link] (mat.210) -| node [pos=0.1, auto] {(0,n)} (mat_pere.south);

\end{tikzpicture}
\end{document}
